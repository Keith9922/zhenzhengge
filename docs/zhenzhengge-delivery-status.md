# 证证鸽实施状态

> 更新时间：`2026-04-20`  
> 用途：记录当前开发进度、已交付能力、已验证链路与剩余缺口，作为产品推进和联调收口依据。

## 1. 当前结论

证证鸽已经从“纯文档阶段”进入“可运行骨架 + 已打通核心联调链路阶段”。

当前已经具备：

- 独立 Git 仓库
- 主站 Landing Page
- 工作台基础壳子
- 浏览器插件基础取证能力
- FastAPI 后端基础接口
- SQLite 持久化
- 插件 -> 后端 -> 工作台 的最小链路
- 草稿、监控、接收方式的真实 API 与页面
- Production 下可运行的主站与工作台

但仍未达到完整产品化状态，主要还缺：

- 用户/组织体系与更细粒度权限模型
- 法证级可信时间戳与电子签章能力
- 平台化投诉材料模板与自动映射

## 2. 已完成内容

## 2.1 文档基线

已完成：

- PRD
- 技术复用清单
- 技术选型表
- 模块架构与开发任务清单
- 信息架构
- API 清单
- 数据库表设计
- Sprint 拆分表

## 2.2 主站与工作台

已完成：

- Landing Page
- 文档中心
- 工作台首页
- 案件列表页
- 案件详情页
- 证据包列表页
- 证据包详情页
- 草稿列表页
- 草稿详情页
- 监控任务页
- 接收方式页
- 通知日志展示
- 模板页
- 设置页

当前状态：

- 可运行
- 可构建
- 可从 API 读案件列表
- Production 下可读取运行时 API
- API 不可用时会显式报错（不再降级到 mock）
- 客户端动作通过站内 proxy 回源到 API

## 2.3 浏览器插件

已完成：

- Plasmo 插件骨架
- Popup 主界面
- 当前页标题、URL、时间戳采集
- 页面正文采集
- HTML 采集
- 可视区截图采集
- 开发模式可显式模拟提交，正式模式下会暴露后端不可用错误

当前状态：

- 可构建
- 可提交取证载荷到后端 intake
- 提交后可返回 `case.case_id` / `evidence_pack.evidence_pack_id`
- 可跳转工作台案件详情
- 正式模式下提交失败会直接暴露错误，不再伪装成功

## 2.4 后端 API

已完成：

- FastAPI 工程骨架
- 健康检查
- 案件列表
- 案件详情
- 案件维度证据包列表
- 证据包列表
- 证据包详情
- 模板列表
- 文书草稿生成、审核、导出
- 监控任务创建、启停、手动执行
- 接收方式创建、测试
- 通知日志列表
- 通知日志前端展示
- runtime 状态接口
- 插件 intake 接口
- 证据包预览与下载接口

当前状态：

- API 可启动
- pytest 可通过
- smoke test 可通过
- 已可接受插件原始 payload
- 已切换为 SQLite 持久化
- 已支持 demo seed 环境开关
- 已接入 MiMo OpenAI 兼容大模型调用
- 已支持真实 Playwright / HTTP 回退抓取
- 已接入项目内 `HermesOrchestrator` 作为后端编排中枢
- 已接入 token 鉴权与角色控制（viewer/operator/admin）
- 已接入组织级 ACL（`organization_id` + `owner_user_id`）并在主要资源接口生效
- 已接入监控调度器与执行历史
- 已接入关键业务动作审计日志
- 文书导出已支持 `.docx` 可编辑版本
- 证据包已接入 RFC3161 可信时间戳能力（可配置 TSA，支持回执下载）
- 草稿详情页已升级富文本编辑器

## 2.5 Git 管理

当前已形成多次小步提交，提交历史清晰，可回滚。

## 3. 已验证链路

已实测通过的链路：

1. 后端健康检查通过
2. Web lint / build 通过
3. 插件 build 通过
4. API pytest / smoke test 通过
5. 手工向 `/api/v1/evidence/intake` 提交插件契约 payload 可成功创建案件和证据包，并返回 `case` / `evidence_pack`
6. 工作台 `/workspace/cases` 已切换到真实 API 数据源
7. 工作台 `/workspace/cases/[caseId]` 可显示真实案件详情与证据包
8. 工作台 `/workspace/drafts` 可生成草稿并跳转详情页
9. 工作台 `/workspace/drafts/[draftId]` 可导出草稿
10. 工作台 `/workspace/monitoring` 可执行监控任务，并在命中后创建案件和证据包
11. 工作台 `/workspace/notifications` 可发送测试消息并写入通知日志
12. 工作台 `/workspace/notifications` 可查看最近通知日志
13. Landing Page、文档入口、工作台入口已通过浏览器验收
14. `next start` 生产态链路已验证可用
15. 证据包详情页已支持截图预览、HTML 片段预览和下载入口

## 4. 当前缺口

### P0 缺口

- 可信时间戳已落地但默认关闭，生产需配置 TSA 并开启强制策略
- 多平台投诉材料导出模板仍不完整（当前以通用文书为主）
- 生产环境可观测性（指标/告警/追踪）仍需加强

### P1 缺口

- 用户注册登录与成员管理体系（当前为 token + 组织作用域 ACL）
- 审核任务中心（指派、SLA、催办）
- 文书模板策略与法务规则库完善

### P2 缺口

- 官方 `NousResearch/hermes-agent` 深度网关/runtime 仍需按生产形态接入
- 历史案例驱动优化
- 飞书 / 企业微信扩展
- 海外站点扩展

## 5. 当前风险

1. 插件与 intake 字段契约已修复，但后续仍需要继续约束返回结构和错误态展示。
2. 当前 API 虽已接 SQLite 与多模块接口，但仍偏 demo 形态，尚未进入正式业务模型。
3. 插件当前是 popup 模式，尚未进入更强的 content script / background 体系。

## 6. 下一步优先级

1. 可信时间戳 + 证据链签名
2. 多平台投诉导出模板
3. 审核任务中心与协作流
4. 生产可观测性与告警
5. 用户/组织体系与资源级权限
