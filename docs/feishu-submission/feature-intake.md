## 2. 关键功能与真实界面

### 2.1 一键取证，把网页内容沉淀成标准证据包

插件侧会采集：

- 当前 URL
- 页面标题
- 页面正文
- 原始 HTML
- 页面截图
- 抓取时间
- 请求编号与备注

后端收到 intake 后，会创建案件、生成证据包，并把截图和 HTML 落到可追溯的存储中。  
在当前演示数据里，`case-zhzg-0007` 已关联证据包 `ep-0005`，并保留了截图预览与 HTML 片段。

